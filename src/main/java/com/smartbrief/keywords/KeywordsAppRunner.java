package com.smartbrief.keywords;

import org.springframework.stereotype.Component;

@Component
public class KeywordsAppRunner {
	/*public static void main(String[] args) throws Exception {
		Collection<UUID> ids = new ArrayList<>();
		for (int i = 0; i < args.length; i++) {
			ids.add(UUID.fromString(args[i]));
		}
		ConfigurableApplicationContext context = new AnnotationConfigApplicationContext("com.smartbrief.keywords");
		KeywordsApp app = context.getBean(KeywordsApp.class);
		app.run(ids);
		context.close();
	}*/
}
